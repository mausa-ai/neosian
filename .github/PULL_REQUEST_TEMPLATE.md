**What became true**

**Why** (the need, the DESIGN section, the ledger row if this departs from a documented decision)

- [ ] `make lint`, `make typecheck`, `make test`, `make size` green with no API key set
- [ ] Tests ride alongside (the shipped fakes, per-test skips)
- [ ] Docs touched where the surface moved (the wheel's docs pages, README, `llms.txt` twins)
- [ ] Every commit is signed off (`git commit -s`, the DCO)
